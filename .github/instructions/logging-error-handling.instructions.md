
# ログ／エラーハンドリング方針

## ログ
- `android.util.Log` を使用。重要な状態遷移や分岐のみ `Log.d/Log.i` を残す。
- デバッグ目的の一時的ログは修正完了前に削除。ただし将来有用なら残してもよい旨をコメントで明示。

## エラー通知
- ユーザー提示が必要なエラーは `UiState` のプロパティまたは `SharedFlow` で UI 層へ伝達。

